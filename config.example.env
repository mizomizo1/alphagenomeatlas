# Copy to .env locally if useful, but do not commit .env.
# The scripts read the API key from the environment.
ALPHA_GENOME_API_KEY=replace_with_your_local_key

# Example local paths. Pass these through CLI arguments rather than importing this file.
INPUT_TSV=/absolute/path/to/all_482_for_avi.tsv
OUTDIR=/absolute/path/to/alphagenome_avi_outputs
